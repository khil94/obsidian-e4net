<%*
const title = await tp.system.prompt("점검할 콘텐츠는","");
const rowCount = await tp.system.prompt("생성할 행(줄)의 개수를 입력하세요", "10");


await tp.file.rename(`${tp.date.now("YYYY-MM-DD")} ${title} 콘텐츠`)

let table = "| 번호 | " + "정답 | 오답" + " |\n";
table += "| --- | " + Array(parseInt(checkboxCount)).fill("---").join(" | ") + " |\n";

for (let i = 1; i <= parseInt(rowCount); i++) {
    let row = `| ${i} | ` + Array(2).fill("- [ ]").join(" | ") + " |";
    table += row + "\n";
}

 tR += table;
%>