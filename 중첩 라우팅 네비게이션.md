
## 아이디어

모든 페이지 url에 대응하여(nested routing nav에 적용될 페이지)
다음과 같은 구조를 만든다.

```typescript
{
	url: 'home',
	name: '홈'
}
```
그리고 url을 parsing하여 이를 nested한 구조로 변환한다.

```typescript
// 예시

url = 'mock-exam/hub/exam-list'

->

urlList = ['hub','exam-list']

let currentUrl = '';
urlList.map(v=>{
	const name = URLToNameRecord[v];
	currentUrl += v;
	return <Link href={`/mock-exam/${currentUrl}`} >
			> {name}
		</Link>
});

```

다만 이러면

## 문제점

폴더 구조를 다시 짜야한다.
모든 페이지는 항상 홈 페이지에서 진입해야 하며, 현재와 같은 hub 페이지는 존재할 수 없다. 정확하게는 sidebar/navbar 로 라우팅 그룹을 나눈 상태에서 hub page가 home page의 역할을 하므로 모든 라우팅 그룹(sidebar 기준)의 페이지는 hub page하위에 존재하여야 한다.

